# Source preparation pipeline to be run from mapterhorn/pipelines folder.
# 5m fallback layer for jphakodatecity1, same scope (see that entry's
# Justfile for the exact bounds and reasoning). 1,403 files, all
# currently published (queried via `aws s3 ls --profile source-coop`).
# source_to_cog.py deliberately skipped, same reasoning as jphakodatecity1.
[no-cd]
default:
    uv run python source_download.py jphakodatecity5m
    uv run python source_bounds.py jphakodatecity5m
    uv run python source_polygonize.py jphakodatecity5m 4
